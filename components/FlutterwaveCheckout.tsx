 'use client';

type Props = Record<string, unknown>;

/**
 * Flutterwave integration removed. Stripe will replace it later.
 */
export default function FlutterwaveCheckout(_props: Props) {
  return null;
}
