'use client';

type Props = Record<string, unknown>;

/**
 * Paystack integration removed. Stripe will replace it later.
 */
export default function PaystackCheckout(_props: Props) {
  return null;
}
